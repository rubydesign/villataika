function load_url(){
  var nights = $("select.nights option:selected").val() ;
  var arriving = $("#arriving").val() ;
  var room = $("#room").val();
//  alert(room);
  return "./bookings/rooms_day.html?nights=" + nights + "&arriving=" + arriving + "&room=" + room;
};
function nights( nights ) { 
  $('#rooms_day').load(load_url());
//  alert( $('#rooms_day').html);
}
function date_select( date , inst ) { 
  $('#rooms_day').load(load_url());
//  alert( $('.rooms_day').html);
}
function set_room(){
  $("#rooms_day .selected").removeClass("selected");
  $(this).addClass( "selected");
  $("#room").val($(this).attr("id"));
}
//$(alert("hi"))
$(document).ready(function() { 
//  alert($('#nights_selected').length + ' elements!');
  $('.nights').change(nights);
  $( "#arriving_on" ).datepicker(
    {
      minDate: 0 ,
      onSelect: function(dateText, inst) { nights(2) ;}
       } 
    );
  $(".free").live("click",  set_room );
});

 