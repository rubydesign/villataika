<% @bookings.each do |room , free| 
  free = true %>
  <td>
    <div class="<%=free ? 'free' : 'taken'%>" id="<%=room%>">
      <%if free%>
      <a href="#"> <%=room.capitalize%> </a>
      <%else%>
      <%= room %>
      <%end%>
    </div>
  </td>
<%end%>
