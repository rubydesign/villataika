<% @bookings.each do |room , free| %>
  <td>
    <div class="<%=free ? 'free' : 'taken'%>">
      <%if free%>
      <a href="#"> <%=room%> </a>
      <%else%>
      <%= room %>
      <%end%>
    </div>
  </td>
<%end%>
